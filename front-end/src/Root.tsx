import React from 'react';
import RefreshComponent from './components/RefreshComponent';

const Root: React.FC = () => {
  return (
    <div>
      <RefreshComponent />
    </div>
  );
};

export default Root;
